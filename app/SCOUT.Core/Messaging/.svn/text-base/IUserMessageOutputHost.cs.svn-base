namespace SCOUT.Core.Messaging
{
    /// <summary>
    /// Defines the method that is called when a message listener receives a 
    /// message that needs be displayed.
    /// </summary>
    public interface IUserMessageOutputHost
    {
        void ProcessMessage(UserMessageEventArgs message);
    }
}